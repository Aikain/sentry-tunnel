name: Run lint

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  golangci:

    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v6.5.0
        with:
          go-version: 1.26
          # We don't need cache when there are no dependencies
          cache: false

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9.3.0
        with:
          only-new-issues: true
